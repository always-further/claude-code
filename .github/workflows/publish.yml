name: Publish nono package

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Publish package
        uses: always-further/agent-sign@a091677a5f90cc44ea11a3305a9e949af6163013 # v0.0.5
        with:
          package-name: claude-code
          package-namespace: always-further
          package-version: ${{ github.ref_name }}
          nono-version: latest
          package-path: .
          registry-url: https://far416w50qhc.share.zrok.io/api/v1
          files: |
            package.json
            claude-code.profile.json
            CLAUDE.md
            hooks/nono-hook.sh
            groups.json
            trust-policy.json
